spring:
  datasource:
    url: jdbc:mysql://${containers-us-west-172.railway.app}:${5539}/${railway}
    username: ${root}
    password: ${eyJl3FlKhqpGldCIMoAZ}
    name: osoblancos.com

  sql:
    init:
      mode: always # you won't do this in prod, I'm just doing this for demo purposes
